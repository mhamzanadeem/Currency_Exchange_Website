services:
  backend:
    build:
      context: ./backend
    env_file: .env
    environment:
      - api_key=${api_key}
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
